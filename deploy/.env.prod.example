# =============================================================================
# Pack — production environment template.
# On the server: cp deploy/.env.prod.example deploy/.env.prod  then fill in real values.
# .env.prod is gitignored — NEVER commit it.
# =============================================================================

# --- Qwen / DashScope (Alibaba Cloud Model Studio) ---
# Console: https://dashscope-intl.aliyuncs.com — Singapore region (ap-southeast-1)
# International endpoint only (NOT the China one ending in .aliyuncs.com without -intl)
QWEN_API_KEY=sk-replace-me
QWEN_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
QWEN_REGION=ap-southeast-1

# Exact model names — verify in the Model Studio console before launch
QWEN_MODEL_MAX=qwen-max
QWEN_MODEL_PLUS=qwen-plus
QWEN_MODEL_FLASH=qwen-turbo

# --- Web Search (Tavily) ---
# Get key at: https://tavily.com — free tier: ~1000 searches/month
SEARCH_PROVIDER=tavily
SEARCH_API_KEY=tvly-replace-me
SEARCH_MAX_RESULTS=5

# --- ApsaraDB RDS for PostgreSQL ---
# Use the VPC-internal endpoint (NOT the public one) — it's faster and free of egress charges
# Format: postgresql://user:password@internal-rds-host:5432/dbname
POSTGRES_URL=postgresql://pack:replace-me@rm-xxxxxxxx.pg.rds.aliyuncs.com:5432/pack
POSTGRES_SSLMODE=require

# --- Alibaba Tair / ApsaraDB for Redis ---
# Use the VPC-internal endpoint. Format: redis://:password@host:6379/0
REDIS_URL=redis://:replace-me@r-xxxxxxxx.redis.rds.aliyuncs.com:6379/0

# --- App ---
# Generate SESSION_SECRET with: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=replace-me-with-64-hex-chars
ENGINE_HOST=0.0.0.0
ENGINE_PORT=8000
GATEWAY_PORT=8080

# --- Spend / boundary ---
FIRST_HUNT_CAP_USD=0.50
STEP_TIMEOUT_S=120.0

# --- Qwen pricing (USD per 1M tokens — update if rates change) ---
PRICE_MAX_IN_PER_M=0.004
PRICE_MAX_OUT_PER_M=0.012
PRICE_PLUS_IN_PER_M=0.0004
PRICE_PLUS_OUT_PER_M=0.0012
PRICE_FLASH_IN_PER_M=0.00015
PRICE_FLASH_OUT_PER_M=0.0006

# --- Resilience ---
QWEN_MAX_RETRIES=3
QWEN_BACKOFF_BASE_S=0.5
QWEN_BREAKER_THRESHOLD=5
QWEN_BREAKER_COOLDOWN_S=30.0

# --- OSS (optional — artifact storage / file uploads) ---
OSS_BUCKET=
OSS_ENDPOINT=
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=
